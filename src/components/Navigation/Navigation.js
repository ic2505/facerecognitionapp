function Navigation() {
  return (
    <nav style={{ display: "flex", justifyContent: "flex-end" }}>
      <p className="f3 link dim black underline pa3 pointer">Sign Out</p>
      {/* f3 = size link = link dim = dims when hover pa3 = padding pointer = cursor */}
    </nav>
  );
}

export default Navigation;
